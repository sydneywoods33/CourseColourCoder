import React from 'react';

function GenerateButton() {
  return (
    <button
      style={{
        padding: '10px 20px',
        fontSize: '1rem',
        color: '#fff',
        backgroundColor: '#39A9DB',
        border: 'none',
        borderRadius: '5px',
        cursor: 'pointer',
      }}
    >
      Generate
    </button>
  );
}

export default GenerateButton;
